sources:
  - name: raw_airbnb
    database: raw
    schema: airbnb

    tables:
      - name: hosts
        identifier: raw_hosts
        
      - name: listings
        identifier: raw_listings
        
      - name: reviews
        identifier: raw_reviews
     